# Retired 2026-09-15.
# The Facebox announcement fired on every doorbell motion (~190 times/week), snapshotted the
# camera, then called image_processing.scan on image_processing.facebox_saved_images, which no
# longer exists (the facebox custom component is gone; HA logged "Referenced entities ... are
# missing" each time). Face recognition now runs through Double Take/CompreFace in
# doorbell_face_greeting.yaml.
