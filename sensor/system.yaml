### SYSTEM ####################################################

# The command_line "HA Uptime" sensor that used to live here stopped loading when the
# command_line platform moved to a top-level key (2023.8). Uptime now comes from the
# System Monitor integration: sensor.system_monitor_last_boot.
