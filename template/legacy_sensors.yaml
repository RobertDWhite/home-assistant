# Legacy template entities removed 2026-09-15:
#   - "Facebox Detection" read image_processing.facebox_face_detection (facebox is gone)
#   - "Dest Address" read input_select.destination (helper no longer exists)
#   - "Dummy" switch wrapped input_boolean.dummy_switch (helper no longer exists)
# Nothing referenced them. Add new template entities as Template Helpers in the UI.
